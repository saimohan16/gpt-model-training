# gpt-model-training
A full end-to-end framework for training GPT  language model from scratch or fine-tuning existing checkpoints. Includes data preprocessing, configurable PyTorch training, DDP support, and text generation tools for research and experimentation.
